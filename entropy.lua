function init (args)
    local needs = {}
    needs["payload"] = tostring(true)
    return needs
end

function match(args)
    p = SCPacketPayload()
    --regex to find domain name
    --calculate max entropy
    --calculate entroy
    --if entropy > 3 and 85% max 
    return 1 
    --else
    return 0
end

